# Dependencies
node_modules/

# Generated native project (regenerated by CI on every build)
android/

# Build artifacts
dist/
*.apk
*.aab

# OS / editor
.DS_Store
Thumbs.db

# Diagnostic captures pulled from the device
# Pulled recordings, including nights renamed to keep more than one around. These are
# hundreds of megabytes; a night worth keeping belongs in fixtures/ as a log, not here.
night*.jsonl
night*.wav
night*.chunks
prefs-backup.xml
*.png
!android-icons/**/*.png
clips/

# Replay harness build output
tools/replay/build/
/night.wav
/night.chunks

# Reference microphone recordings
reference/
